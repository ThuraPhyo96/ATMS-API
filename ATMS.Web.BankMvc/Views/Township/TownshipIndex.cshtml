@model List<TownshipViewModel>

<a asp-controller="Township" asp-action="Create" class="btn btn-primary">New</a>

<table id="townshipGrid" class="display" style="width:100%">
    <thead>
        <tr>
           @*  <th></th> *@
            <th>Region</th>
            <th>Division</th>
            <th>Township</th>
            <th>Description</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
               @*  <th>
                    <a asp-controller="ATMLocation" asp-action="Edit" asp-route-id="@item.ATMLocationId" class="btn btn-secondary">Edit</a>
                    <button type="button" class="btn btn-danger btnDelete" data-id="@item.ATMLocationId">Delete</button>
                </th> *@
                <td>@item.RegionName</td>
                <td>@item.DivisionName</td>
                <td>@item.Name</td>
                <td>@item.Description</td>
            </tr>
        }
    </tbody>
</table>

@section Scripts
{
    <script src="~/scripts/township/townshipindex.js" asp-append-version="true"></script>
}